# 🚀 24小时云端部署指南

## 🎯 最快部署方案（5分钟）

### 方案1: Zeabur（推荐，中国最快）⭐⭐⭐⭐⭐

**优势**:
- ✅ 中国访问速度最快
- ✅ 免费额度充足
- ✅ 支持WebSocket
- ✅ 自动HTTPS
- ✅ 一键部署

**步骤**:

1. **访问 Zeabur**
   ```
   https://zeabur.com
   ```

2. **GitHub登录**
   - 使用GitHub账号登录

3. **创建项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub"
   - 选择仓库 `ai-job-helper`

4. **配置环境变量**
   ```
   DEEPSEEK_API_KEY=你的API密钥
   PORT=8000
   # 可选：开启实时岗位数据（Jooble）
   JOOBLE_API_KEY=你的Jooble API Key
   JOB_DATA_PROVIDER=auto
   ```

5. **部署**
   - 点击 "Deploy"
   - 等待2分钟

6. **获取域名**
   - 自动分配域名: `https://你的项目.zeabur.app`
   - 可绑定自定义域名

---

### 方案2: Railway（备选）⭐⭐⭐⭐

**步骤**:

```bash
# 1. 安装Railway CLI
npm i -g @railway/cli

# 2. 登录
railway login

# 3. 初始化
railway init

# 4. 添加环境变量
railway variables set DEEPSEEK_API_KEY=你的密钥

# 5. 部署
railway up

# 6. 获取域名
railway domain
```

**访问**: `https://你的项目.railway.app`

---

### 方案3: Render（备选）⭐⭐⭐⭐

**步骤**:

1. 访问 https://render.com
2. 连接GitHub仓库
3. 选择 "Web Service"
4. 配置:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python web_app.py`
5. 添加环境变量 `DEEPSEEK_API_KEY`
6. 点击 "Create Web Service"

**访问**: `https://你的项目.onrender.com`

---

## 🔄 自动部署流程

### 每次修改后自动部署

**Windows**:
```cmd
双击运行: 自动部署.bat
```

**Linux/Mac**:
```bash
chmod +x auto-deploy.sh
./auto-deploy.sh
```

### 工作流程
```
1. 修改代码
2. 运行自动部署脚本
3. 自动提交到GitHub
4. 云平台自动检测并部署
5. 2分钟后访问新版本
```

---

## 📝 详细步骤（首次部署）

### 步骤1: 创建GitHub仓库

1. 访问 https://github.com/new
2. 仓库名称: `ai-job-helper`
3. 设置为 Public
4. 不要勾选任何初始化选项
5. 点击 "Create repository"

### 步骤2: 推送代码

```bash
cd C:\Users\陈盈桦\Desktop\自动投简历

# 初始化Git
git init
git add .
git commit -m "初始提交"
git branch -M main

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/ai-job-helper.git

# 推送
git push -u origin main
```

### 步骤3: 部署到Zeabur

1. 访问 https://zeabur.com
2. 使用GitHub登录
3. 点击 "New Project"
4. 选择 "Deploy from GitHub"
5. 选择 `ai-job-helper` 仓库
6. 添加环境变量:
   ```
   DEEPSEEK_API_KEY=你的密钥
   ```
7. 点击 "Deploy"
8. 等待部署完成

### 步骤4: 获取访问地址

部署完成后，Zeabur会自动分配域名：
```
https://ai-job-helper-xxxx.zeabur.app
```

---

## 🌐 访问测试

### 测试清单
- [ ] 访问首页: `https://你的域名.zeabur.app`
- [ ] 点击"应用": `https://你的域名.zeabur.app/app`
- [ ] 上传简历测试
- [ ] 查看实时进度
- [ ] 查看结果
- [ ] 下载结果

---

## 🔧 常见问题

### Q1: 部署失败
**A**: 检查 `requirements.txt` 是否完整，确保包含所有依赖

### Q2: 访问很慢
**A**: 使用Zeabur，中国访问速度最快

### Q3: WebSocket连接失败
**A**: 确保云平台支持WebSocket（Zeabur和Railway支持）

### Q4: 环境变量未生效
**A**: 在云平台控制台重新添加环境变量并重新部署

---

## 📊 成本对比

| 平台 | 月费用 | 流量 | 中国访问 | WebSocket | 推荐度 |
|------|--------|------|----------|-----------|--------|
| **Zeabur** | $0 (免费) | 100GB | ✅✅ 快 | ✅ | ⭐⭐⭐⭐⭐ |
| Railway | $5 (免费) | 100GB | ✅ 中等 | ✅ | ⭐⭐⭐⭐ |
| Render | $0 (免费) | 100GB | ✅ 中等 | ✅ | ⭐⭐⭐⭐ |
| Vercel | $0 (免费) | 无限 | ✅ 快 | ❌ | ⭐⭐⭐ |

**推荐**: Zeabur（中国访问最快，免费，支持WebSocket）

---

## 🎯 快速命令

### 一键部署到Zeabur
```bash
# 1. 提交代码
git add . && git commit -m "更新" && git push

# 2. Zeabur会自动检测并部署
# 无需手动操作！
```

### 一键部署到Railway
```bash
railway up
```

### 一键部署到Render
```bash
# 推送到GitHub后自动部署
git push
```

---

## 🎉 部署完成

部署完成后，你将获得：

1. ✅ **24小时在线访问**
2. ✅ **自动HTTPS**
3. ✅ **中国高速访问**
4. ✅ **自动更新部署**
5. ✅ **免费使用**

**访问地址**: `https://你的域名.zeabur.app`

---

**现在运行 `自动部署.bat` 开始部署！** 🚀

