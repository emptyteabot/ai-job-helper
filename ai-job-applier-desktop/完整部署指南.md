# 🚀 AI 求职助手 - 完整部署指南

## 📋 当前状态

✅ **后端服务**：已启动（端口 8765，进程 36800）
✅ **ngrok 隧道**：已启动（进程 22048）
✅ **前端代码**：已推送到 GitHub
✅ **Streamlit Cloud**：正在部署

---

## 🔧 解决 HTTP 422 错误

### 问题原因
ngrok 免费版会显示警告页面，需要点击 "Visit Site" 才能访问后端 API。

### 解决方案（3 选 1）

#### 方案 1：使用 ngrok 付费版（推荐）
```bash
# 注册 ngrok 账号并获取 authtoken
ngrok config add-authtoken YOUR_TOKEN

# 启动时添加 --domain 参数
ngrok http 8765 --domain=your-custom-domain.ngrok-free.app
```

**优势**：
- 无警告页面
- 固定域名
- 更稳定

**价格**：约 $8/月

---

#### 方案 2：使用 Cloudflare Tunnel（免费 + 推荐）
```bash
# 1. 安装 cloudflared
# 下载：https://github.com/cloudflare/cloudflared/releases

# 2. 登录 Cloudflare
cloudflared tunnel login

# 3. 创建隧道
cloudflared tunnel create ai-job-helper

# 4. 启动隧道
cloudflared tunnel --url http://localhost:8765
```

**优势**：
- ✅ 完全免费
- ✅ 无警告页面
- ✅ 更快速度
- ✅ 更好安全性

---

#### 方案 3：部署到云服务器（生产环境推荐）

##### 3.1 阿里云/腾讯云轻量服务器
**价格**：¥99/年（2核2G）

**步骤**：
```bash
# 1. 购买服务器（Ubuntu 22.04）

# 2. SSH 连接服务器
ssh root@your-server-ip

# 3. 安装依赖
apt update
apt install -y python3 python3-pip git

# 4. 克隆代码
git clone https://github.com/emptyteabot/ai-job-helper.git
cd ai-job-helper/backend

# 5. 安装 Python 依赖
pip3 install -r requirements.txt

# 6. 安装 Playwright
playwright install chromium
playwright install-deps

# 7. 启动服务（后台运行）
nohup python3 main.py --port 8765 > backend.log 2>&1 &

# 8. 配置防火墙
ufw allow 8765
```

**前端配置**：
```python
# streamlit_app.py
BACKEND_URL = "http://your-server-ip:8765"
```

---

## 🎯 快速测试（本地）

### 1. 测试后端 API
```bash
# 打开浏览器访问
http://localhost:8765/docs

# 或使用 curl
curl http://localhost:8765/health
```

### 2. 测试投递功能
```bash
# 使用 Postman 或 curl
curl -X POST "http://localhost:8765/api/apply/boss/batch?keyword=Python开发&city=北京&max_count=5&greeting_template=您好"
```

---

## 📱 推荐方案对比

| 方案 | 价格 | 稳定性 | 速度 | 难度 |
|------|------|--------|------|------|
| ngrok 免费版 | 免费 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐ |
| ngrok 付费版 | $8/月 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐ |
| Cloudflare Tunnel | 免费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| 云服务器 | ¥99/年 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🔥 我的建议

### 测试阶段（现在）
使用 **Cloudflare Tunnel**：
- 免费
- 无警告页面
- 5 分钟搞定

### 生产阶段（上线后）
使用 **阿里云轻量服务器**：
- ¥99/年
- 24 小时稳定运行
- 可以跑多个项目

---

## 📝 下一步操作

### 立即可用（Cloudflare Tunnel）

1. **下载 cloudflared**
   ```bash
   # Windows
   # 访问：https://github.com/cloudflare/cloudflared/releases
   # 下载 cloudflared-windows-amd64.exe
   ```

2. **启动隧道**
   ```bash
   cloudflared tunnel --url http://localhost:8765
   ```

3. **复制公网地址**
   ```
   例如：https://abc-def-ghi.trycloudflare.com
   ```

4. **更新前端配置**
   ```python
   # ai-job-helper/streamlit_app.py
   BACKEND_URL = "https://abc-def-ghi.trycloudflare.com"
   ```

5. **推送到 GitHub**
   ```bash
   cd ai-job-helper
   git add streamlit_app.py
   git commit -m "update backend url"
   git push
   ```

6. **等待 Streamlit Cloud 部署**（1-2 分钟）

7. **测试完整流程**
   - 上传简历 → AI 分析 → 自动投递

---

## ❓ 常见问题

### Q1: 为什么 ngrok 显示警告页面？
A: ngrok 免费版的限制，需要手动点击 "Visit Site"。建议使用 Cloudflare Tunnel。

### Q2: 后端服务会自动停止吗？
A: 如果关闭命令行窗口会停止。建议：
- Windows：使用 `start /b python main.py`
- Linux：使用 `nohup python main.py &`

### Q3: 如何查看后端日志？
A: 
```bash
# 查看实时日志
tail -f backend/backend.log

# 或在 Windows 查看
type backend\backend.log
```

### Q4: 投递失败怎么办？
A: 检查：
1. 后端服务是否运行
2. ngrok/cloudflare 隧道是否正常
3. Boss 直聘是否已登录
4. 网络连接是否正常

---

## 🎉 总结

你现在有一个完整的 **Web SaaS 架构**：

```
用户浏览器（Streamlit Cloud）
    ↓
公网隧道（ngrok/cloudflare）
    ↓
你的电脑（24h 后端服务）
    ↓
Playwright 自动化投递
```

**下一步**：选择一个隧道方案，更新前端配置，就可以开始自动投递了！🚀

