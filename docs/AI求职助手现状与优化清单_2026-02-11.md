# AI求职助手现状与优化清单（2026-02-11）

## 当前进度（已完成）
- 推荐岗位链路已切到真实数据优先，默认云端可运行回退（当前实测为 `remotive`）。
- 明确过滤演示岗位（`seed/demo`）与无跳转链接岗位，不再展示硬编码示例公司。
- `GET /api/process` 已稳定返回 JSON，并含 `job_provider_mode` 字段。
- 前端已重做为简洁风格（`/` 和 `/app`），并修复 UTF-8 乱码问题。
- GitHub Actions `deploy.yml` 已修复重复定义问题，不再出现 `on/jobs already defined` 错误。
- 已移除 `web_app.py` 中未使用的旧 `JobSearcher` mock 依赖，降低“假数据误回退”风险。

## 本地验证结果（本次改动后）
- `GET /` -> `200`，可正常打开首页。
- `GET /app` -> `200`，可正常打开分析工作台。
- `GET /api/version` -> `200`，返回标准 JSON。
- `GET /api/jobs/search?keywords=Python&location=北京&limit=1` -> `200`，`provider_mode=remotive`，返回真实链接。
- `POST /api/process` -> `200`，返回 JSON，包含 `job_recommendations` 且显示真实来源标题。

## 线上部署状态（Railway）
- 最新代码已推送 `main`，提交：`04a61c2`。
- Railway 会自动拉取并部署。
- 校验方式：
  - 访问 `/api/version`
  - 访问 `/app` 看是否是简洁新界面
  - 上传简历后看推荐岗位标题是否含“来自 Remotive 公共招聘 API”或其他真实来源

## 目前仍存在的产品级短板
- 数据源质量：Remotive 偏海外远程岗位，和中国本地求职场景有偏差。
- 岗位个性化：当前匹配分仍偏轻量，尚未基于更细粒度技能图谱做重排。
- 可观测性：虽然有健康接口，但缺“最近抓取成功率/失败原因趋势”的仪表盘。
- 商业化闭环：有线索接口，但缺自动化转化漏斗看板和告警。

## 下一步优化（按融资优先级）

### P0（1-2天，必须做）
1. 做“数据源优先级策略”配置化
- 目标：云端优先中国可用来源，其次 Remotive。
- 验收：`/api/jobs/search` 返回里有明确 `provider_mode` 与 `warning`。

2. 做“岗位质量闸门”
- 规则：必须有可跳转链接、标题长度阈值、公司名非空、去重。
- 验收：推荐列表中无占位项、无死链接。

3. 做“无数据兜底文案”统一
- 目标：前端不再出现误导文案，明确提示“当前无真实岗位+排查建议”。
- 验收：空数据时不显示任何示例岗位。

### P1（3-5天，建议做）
1. 增加“来源混合重排”
- 来源：Remotive + 搜索引擎抓取 + 你的云缓存。
- 逻辑：按技能匹配度、地点匹配、发布时间综合排序。

2. 增加“增长看板接口”
- 指标：上传率、分析完成率、搜索率、投递率、留资率。
- 目标：可直接用于融资材料和周复盘。

3. 前端输出结构化卡片
- 从纯文本改为卡片列表（标题/公司/地点/薪资/匹配度/链接/来源）。

### P2（7天+）
1. 接入中国可持续来源（合规前提下）
- 目标：提升中国岗位覆盖率和转化率。

2. 建立 A/B 测试（推荐排序、简历优化模板）
- 目标：用数据证明“转化提升”。

## 你可以直接这样给我下需求
```text
需求标题：
优先级：P0/P1/P2
目标用户：
验收标准（量化）：
上线截止时间：
```

## 我建议你下一条就让我做
1. `P0-1+P0-2` 一次性并行完成（数据源策略 + 岗位质量闸门）。
2. 同步把 `/app` 的岗位结果改成结构化卡片（不是纯文本）。
