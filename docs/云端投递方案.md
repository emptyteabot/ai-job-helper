# 云端投递方案 - 无需本地安装

## 目标

让大部分用户能直接在网页上投递，无需在电脑上安装 OpenClaw 或 Selenium。

---

## 三种实现方案

### 方案1：邮件 + 在线投递链接（推荐）⭐

**流程：**
```
用户填写手机号/邮箱
    ↓
生成投递配置
    ↓
发送邮件（包含在线投递链接）
    ↓
用户点击链接
    ↓
授权登录招聘平台
    ↓
云端自动投递
    ↓
结果发送到邮箱
```

**优势：**
- ✅ 用户体验最好
- ✅ 无需安装任何软件
- ✅ 手机也能用
- ✅ 安全可靠（OAuth 授权）

**技术实现：**

1. **前端（Streamlit）**
   ```python
   # 用户填写信息
   user_phone = st.text_input("手机号")
   user_email = st.text_input("邮箱")

   # 提交任务
   if st.button("提交投递任务"):
       # 生成 token
       token = generate_token(user_email, user_phone)

       # 发送邮件
       send_email(
           to=user_email,
           subject="您的投递方案已生成",
           body=f"点击链接开始投递：https://your-service.com/apply?token={token}"
       )
   ```

2. **在线投递页面（Streamlit Pages）**
   ```python
   # pages/online_apply.py
   def render_apply_page(token):
       # 验证 token
       user_data = verify_token(token)

       # 步骤1：选择平台
       platform = st.selectbox("选择平台", ["Boss直聘", "实习僧"])

       # 步骤2：授权登录
       if platform == "Boss直聘":
           qr_code = generate_qr_code()
           st.image(qr_code)

       # 步骤3：开始投递
       if st.button("开始投递"):
           start_apply_task(user_data, platform)
   ```

3. **后端投递服务（云函数）**
   ```python
   # 使用阿里云函数/腾讯云函数
   def apply_handler(event, context):
       user_data = event['user_data']
       platform = event['platform']

       # 使用 Selenium 投递
       driver = init_driver()
       login_with_oauth(driver, user_data['oauth_token'])

       for job in search_jobs(driver, user_data['targets']):
           apply_job(driver, job)

       # 发送结果邮件
       send_result_email(user_data['email'])
   ```

**成本：**
- 阿里云函数：0.0000167 元/GB·秒
- 每次投递约 30 秒，成本 < 0.01 元
- 每月 100 次投递 < 1 元

---

### 方案2：GitHub Actions（免费）⭐⭐

**流程：**
```
用户填写信息
    ↓
生成投递脚本
    ↓
触发 GitHub Actions
    ↓
Actions 运行 Selenium 脚本
    ↓
结果发送到邮箱
```

**优势：**
- ✅ 完全免费（每月 2000 分钟）
- ✅ 无需自己的服务器
- ✅ GitHub 官方支持

**技术实现：**

1. **创建 GitHub Actions 工作流**
   ```yaml
   # .github/workflows/auto_apply.yml
   name: Auto Apply Job

   on:
     workflow_dispatch:
       inputs:
         user_email:
           description: '用户邮箱'
           required: true
         targets:
           description: '投递目标（JSON）'
           required: true
         platform:
           description: '平台'
           required: true

   jobs:
     apply:
       runs-on: ubuntu-latest
       steps:
         - name: Checkout
           uses: actions/checkout@v3

         - name: Setup Python
           uses: actions/setup-python@v4
           with:
             python-version: '3.10'

         - name: Install dependencies
           run: |
             pip install selenium undetected-chromedriver

         - name: Run apply script
           run: |
             python scripts/auto_apply.py \
               --email "${{ github.event.inputs.user_email }}" \
               --targets '${{ github.event.inputs.targets }}' \
               --platform "${{ github.event.inputs.platform }}"

         - name: Send result email
           run: |
             python scripts/send_email.py \
               --to "${{ github.event.inputs.user_email }}"
   ```

2. **触发 Actions**
   ```python
   import requests

   def trigger_github_actions(user_data, targets, platform):
       url = "https://api.github.com/repos/your-repo/actions/workflows/auto_apply.yml/dispatches"

       headers = {
           "Authorization": f"token {GITHUB_TOKEN}",
           "Accept": "application/vnd.github.v3+json"
       }

       data = {
           "ref": "main",
           "inputs": {
               "user_email": user_data['email'],
               "targets": json.dumps(targets),
               "platform": platform
           }
       }

       response = requests.post(url, headers=headers, json=data)
       return response.json()
   ```

**限制：**
- 每月 2000 分钟（约 66 小时）
- 每次投递约 10-20 分钟
- 每月可支持 100-200 次投递

---

### 方案3：Webhook + 第三方平台

**流程：**
```
用户填写信息
    ↓
发送到 Webhook
    ↓
Zapier/Make 接收
    ↓
触发投递脚本
    ↓
结果发送到邮箱
```

**支持的平台：**
- Zapier（付费）
- Make (Integromat)（有免费额度）
- n8n（开源，需自建）

**优势：**
- ✅ 可视化配置
- ✅ 无需写代码
- ✅ 集成丰富

**劣势：**
- ❌ 免费额度有限
- ❌ 不支持复杂逻辑

---

## 推荐方案组合

### 给普通用户：方案1（邮件 + 在线投递）

**适合人群：**
- 不懂技术的用户
- 想要最简单体验的用户
- 愿意付费的用户（9.9 元/月）

**实现步骤：**

1. **Streamlit 前端**
   - 用户填写手机号/邮箱
   - 提交投递任务
   - 显示"邮件已发送"

2. **发送邮件**
   - 使用 SendGrid/阿里云邮件服务
   - 邮件包含在线投递链接

3. **在线投递页面**
   - 用户点击链接
   - 授权登录（扫码/验证码）
   - 开始投递

4. **云函数投递**
   - 阿里云函数/腾讯云函数
   - 运行 Selenium 脚本
   - 发送结果邮件

**成本估算：**
- 云函数：1 元/月（100 次）
- 邮件服务：免费（SendGrid 每月 100 封免费）
- 总成本：< 2 元/月

**定价：**
- 免费版：每天 10 个岗位
- 基础版：9.9 元/月，每天 30 个
- 高级版：19.9 元/月，每天 50 个

---

### 给技术用户：方案2（GitHub Actions）

**适合人群：**
- 懂技术的用户
- 想要免费的用户
- 有 GitHub 账号的用户

**实现步骤：**

1. **Fork 项目**
   ```bash
   git clone https://github.com/your-repo/ai-job-helper.git
   ```

2. **配置 Secrets**
   - 在 GitHub 仓库设置中添加：
     - `USER_EMAIL`
     - `USER_PHONE`
     - `TARGETS`（JSON 格式）

3. **手动触发 Actions**
   - 在 GitHub 仓库的 Actions 页面
   - 点击 "Run workflow"
   - 填写参数
   - 开始投递

4. **查看结果**
   - Actions 运行日志
   - 邮件通知

---

## 技术细节

### 1. OAuth 授权登录

**Boss直聘扫码登录：**
```python
def generate_qr_code(token):
    # 调用 Boss 直聘 API 生成二维码
    response = requests.post(
        "https://www.zhipin.com/wapi/zppassport/qrcode/dispatcher",
        json={"token": token}
    )
    qr_url = response.json()['zpData']['qrCodeUrl']
    return qr_url

def check_qr_status(token):
    # 轮询检查扫码状态
    response = requests.get(
        f"https://www.zhipin.com/wapi/zppassport/qrcode/status?token={token}"
    )
    status = response.json()['zpData']['status']
    # 0: 未扫码, 1: 已扫码, 2: 已确认
    return status
```

**手机验证码登录：**
```python
def send_sms_code(phone):
    response = requests.post(
        "https://www.zhipin.com/wapi/zppassport/captcha/sendSms",
        json={"phone": phone}
    )
    return response.json()

def login_with_sms(phone, code):
    response = requests.post(
        "https://www.zhipin.com/wapi/zppassport/login/sms",
        json={"phone": phone, "code": code}
    )
    token = response.json()['zpData']['token']
    return token
```

### 2. 云函数部署

**阿里云函数：**
```python
# index.py
import json
from selenium import webdriver

def handler(event, context):
    # 解析参数
    body = json.loads(event['body'])
    user_data = body['user_data']
    platform = body['platform']

    # 初始化 Selenium
    options = webdriver.ChromeOptions()
    options.add_argument('--headless')
    options.add_argument('--no-sandbox')
    driver = webdriver.Chrome(options=options)

    # 投递逻辑
    results = apply_jobs(driver, user_data, platform)

    # 返回结果
    return {
        'statusCode': 200,
        'body': json.dumps(results)
    }
```

**部署命令：**
```bash
# 安装 Serverless 工具
npm install -g @serverless-devs/s

# 部署
s deploy
```

### 3. 邮件服务

**使用 SendGrid：**
```python
import sendgrid
from sendgrid.helpers.mail import Mail

def send_apply_email(to_email, apply_url, targets):
    sg = sendgrid.SendGridAPIClient(api_key=SENDGRID_API_KEY)

    message = Mail(
        from_email='noreply@your-service.com',
        to_emails=to_email,
        subject='您的投递方案已生成',
        html_content=f'''
        <h2>🎯 您的智能投递方案已生成</h2>

        <h3>📊 投递策略</h3>
        <ul>
            <li>关键词：{', '.join(targets['keywords'])}</li>
            <li>地点：{', '.join(targets['locations'])}</li>
        </ul>

        <a href="{apply_url}" style="
            display: inline-block;
            padding: 12px 24px;
            background: linear-gradient(135deg, #ffb6b9 0%, #fae3d9 100%);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: bold;
        ">🚀 立即开始投递</a>

        <p>祝你找到心仪的实习！</p>
        '''
    )

    response = sg.send(message)
    return response.status_code
```

---

## 实施计划

### 第一阶段：MVP（最小可行产品）

**功能：**
- ✅ 用户填写手机号/邮箱
- ✅ 发送邮件（包含投递脚本）
- ✅ 用户本地运行脚本

**时间：** 1 天

### 第二阶段：在线投递

**功能：**
- ✅ 在线投递页面
- ✅ 扫码登录
- ✅ 云函数投递

**时间：** 3-5 天

### 第三阶段：优化体验

**功能：**
- ✅ 实时进度显示
- ✅ 投递报告
- ✅ 数据统计

**时间：** 2-3 天

---

## 总结

**最佳方案：邮件 + 在线投递**

**优势：**
- 用户体验最好
- 无需安装软件
- 手机也能用
- 可以收费（9.9 元/月）

**实现难度：** 中等

**成本：** < 2 元/月

**适合人群：** 所有用户

**下一步：**
1. 实现邮件发送功能
2. 创建在线投递页面
3. 部署云函数
4. 测试完整流程
