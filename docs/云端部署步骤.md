# 🚀 云端部署完整指南（Railway/Render）

## 📋 部署前准备

### ✅ 检查清单
- [x] requirements.txt - Python依赖
- [x] web_app.py - Web服务
- [x] Procfile - 启动命令
- [x] railway.json - Railway配置
- [x] render.yaml - Render配置
- [x] runtime.txt - Python版本

### 🔑 需要准备的信息
1. **DeepSeek API Key** - [获取地址](https://platform.deepseek.com/api_keys)
2. **爬虫API密钥** - 稍后生成

---

## 🎯 方案一：Railway 部署（推荐）

### 为什么选择 Railway？
- ✅ 免费额度：500小时/月（足够24小时运行）
- ✅ 部署简单：一键部署
- ✅ 自动HTTPS：自动配置域名和证书
- ✅ 日志查看：实时查看运行日志

### 步骤1：安装 Railway CLI

```bash
# Windows (使用 npm)
npm install -g @railway/cli

# 或者使用 Scoop
scoop install railway
```

### 步骤2：登录 Railway

```bash
# 打开命令行，运行
railway login

# 会自动打开浏览器，使用GitHub账号登录
```

### 步骤3：初始化项目

```bash
# 进入项目目录
cd "c:\Users\陈盈桦\Desktop\Desktop_整理_2026-02-09_172732\Folders\自动投简历"

# 初始化Railway项目
railway init

# 选择：Create a new project
# 输入项目名称：ai-job-helper
```

### 步骤4：配置环境变量

```bash
# 生成安全的API密钥
python -c "import secrets; print(secrets.token_urlsafe(32))"
# 复制输出的密钥，例如：abc123xyz456...

# 设置环境变量
railway variables set DEEPSEEK_API_KEY=sk-你的DeepSeek密钥
railway variables set CRAWLER_API_KEY=刚才生成的密钥
railway variables set JOB_DATA_PROVIDER=cloud
railway variables set PORT=8000
```

### 步骤5：部署

```bash
# 部署到Railway
railway up

# 等待部署完成（约2-3分钟）
```

### 步骤6：获取URL

```bash
# 生成公开URL
railway domain

# 会返回类似：
# https://ai-job-helper-production.up.railway.app
```

### 步骤7：验证部署

访问：`https://你的域名/api/health`

应该返回：
```json
{
  "status": "ok",
  "message": "AI求职助手运行正常"
}
```

---

## 🎯 方案二：Render 部署

### 步骤1：创建 GitHub 仓库

```bash
# 1. 在GitHub创建新仓库：ai-job-helper

# 2. 初始化Git（如果还没有）
cd "c:\Users\陈盈桦\Desktop\Desktop_整理_2026-02-09_172732\Folders\自动投简历"
git init
git add .
git commit -m "Initial commit"

# 3. 推送到GitHub
git remote add origin https://github.com/你的用户名/ai-job-helper.git
git branch -M main
git push -u origin main
```

### 步骤2：连接 Render

1. 访问 [Render.com](https://render.com)
2. 使用GitHub账号登录
3. 点击 "New +" → "Web Service"
4. 选择您的 `ai-job-helper` 仓库

### 步骤3：配置服务

```
Name: ai-job-helper
Environment: Python
Build Command: pip install -r requirements.txt
Start Command: python web_app.py
```

### 步骤4：添加环境变量

在 "Environment" 标签页添加：

```
DEEPSEEK_API_KEY = sk-你的DeepSeek密钥
CRAWLER_API_KEY = 生成的安全密钥
JOB_DATA_PROVIDER = cloud
PORT = 8000
```

### 步骤5：部署

点击 "Create Web Service"，等待部署完成（约5分钟）

### 步骤6：获取URL

部署完成后，会得到类似：
```
https://ai-job-helper.onrender.com
```

---

## 🎯 方案三：Vercel 部署（不推荐）

⚠️ **注意**：Vercel 主要用于静态网站和Serverless函数，不适合长时间运行的Web服务。

如果仍要使用：

```bash
# 安装Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
vercel --prod
```

---

## 📝 部署后配置

### 1. 记录您的云端URL

```
我的云端URL：https://_____________________.railway.app
```

### 2. 生成并保存API密钥

```bash
# 生成密钥
python -c "import secrets; print(secrets.token_urlsafe(32))"

# 保存到安全的地方
我的爬虫API密钥：_____________________________________
```

### 3. 测试云端服务

```bash
# 测试健康检查
curl https://你的域名/api/health

# 测试爬虫接口（应该返回401未授权）
curl https://你的域名/api/crawler/status
```

---

## 🔍 验证部署成功

### ✅ 检查清单

1. **访问首页**
   ```
   https://你的域名/
   应该看到：AI求职助手首页
   ```

2. **访问应用页面**
   ```
   https://你的域名/app
   应该看到：上传简历界面
   ```

3. **测试健康检查**
   ```
   https://你的域名/api/health
   应该返回：{"status": "ok"}
   ```

4. **测试AI分析**
   - 访问 `/app`
   - 上传简历或输入文本
   - 点击"开始AI分析"
   - 应该能看到分析结果

---

## 🛠️ 常见问题

### Q1: 部署失败，显示"Build failed"

**检查：**
1. requirements.txt 是否正确
2. Python版本是否兼容（建议3.11）
3. 查看构建日志找到具体错误

**解决：**
```bash
# 本地测试
pip install -r requirements.txt
python web_app.py

# 如果本地正常，检查云端日志
railway logs  # Railway
# 或在Render控制台查看日志
```

### Q2: 部署成功但访问报错

**检查：**
1. 环境变量是否配置正确
2. DEEPSEEK_API_KEY 是否有效
3. PORT 是否设置为 8000

**解决：**
```bash
# Railway查看日志
railway logs

# 检查环境变量
railway variables
```

### Q3: AI分析失败

**原因：**
- DEEPSEEK_API_KEY 未配置或无效
- API额度用完

**解决：**
1. 访问 https://platform.deepseek.com
2. 检查API Key是否有效
3. 检查账户余额

### Q4: 如何查看日志？

**Railway：**
```bash
# 命令行查看
railway logs

# 或访问控制台
https://railway.app/dashboard
```

**Render：**
- 访问 Render 控制台
- 点击您的服务
- 查看 "Logs" 标签页

---

## 📊 监控和维护

### 1. 设置监控

**Railway：**
- 自动监控服务状态
- 异常时会自动重启

**Render：**
- 免费版会在15分钟无请求后休眠
- 付费版可以保持常驻

### 2. 查看使用情况

**Railway：**
```bash
# 查看使用时间
railway status
```

**Render：**
- 访问控制台查看使用统计

### 3. 更新部署

**Railway：**
```bash
# 修改代码后重新部署
railway up
```

**Render：**
- 推送到GitHub会自动部署
```bash
git add .
git commit -m "Update"
git push
```

---

## 🎉 部署完成！

### 下一步：

1. ✅ **记录您的云端URL**
   ```
   https://你的域名
   ```

2. ✅ **保存API密钥**
   ```
   CRAWLER_API_KEY=你的密钥
   ```

3. ✅ **配置本地爬虫**
   - 编辑 `crawler.env`
   - 填入云端URL和API密钥

4. ✅ **启动本地爬虫**
   ```bash
   双击运行：启动爬虫服务.bat
   ```

5. ✅ **分享给用户**
   ```
   把您的云端URL分享给其他人
   他们就可以使用AI求职助手了！
   ```

---

## 📞 获取帮助

- 📖 Railway文档：https://docs.railway.app
- 📖 Render文档：https://render.com/docs
- 🐛 遇到问题：查看日志或联系我

---

**祝您部署顺利！🚀**

