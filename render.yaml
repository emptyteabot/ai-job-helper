services:
  - type: web
    name: ai-job-helper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: PORT
        value: 8000

