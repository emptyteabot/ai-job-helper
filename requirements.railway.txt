# ========================================
# AI求职助手 - 核心依赖（Railway 部署用）
# ========================================

# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0
pytest>=8.0.0

# 环境配置
python-dotenv>=1.0.0

# AI接口
openai>=1.3.0

# HTTP请求
requests>=2.32.0

# 文档解析
PyPDF2>=3.0.0
python-docx>=1.1.0

# 图片处理
Pillow>=10.4.0
pytesseract>=0.3.10

# 定时任务
schedule>=1.2.0

# 性能监控
psutil>=5.9.0

# 自动投递依赖（基础）
selenium>=4.15.0
webdriver-manager>=4.0.1

# 注意：以下依赖在 Railway 环境可能需要额外配置
# 如果部署失败，可以注释掉这些依赖
# undetected-chromedriver>=3.5.4
# DrissionPage>=4.0.0
# ddddocr>=1.4.0
