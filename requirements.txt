# ========================================
# AI求职助手 - Python依赖包
# ========================================

# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# 测试框架
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# 环境配置
python-dotenv>=1.0.0

# AI接口
openai>=1.3.0

# HTTP请求
requests>=2.32.0

# 文档解析
PyPDF2>=3.0.0
python-docx>=1.1.0

# 图片处理（兼容Python 3.13）
Pillow>=10.4.0
pytesseract>=0.3.10

# 定时任务（用于爬虫服务）
schedule>=1.2.0

# 性能监控
psutil>=5.9.0

# 自动投递依赖
selenium>=4.15.0
undetected-chromedriver>=3.5.4
webdriver-manager>=4.0.1

# Playwright - 现代浏览器自动化
playwright>=1.40.0
playwright-stealth>=1.0.0

# DrissionPage - 国产高速浏览器自动化框架
DrissionPage>=4.0.0

# 验证码识别（可选）
ddddocr>=1.4.0

# 缓存（可选，生产环境推荐）
# redis>=5.0.0

# ========================================
# 可选依赖（需要单独安装）
# ========================================

# OpenClaw - 浏览器自动化（用于抓取Boss直聘真实岗位）
# 安装方法：
#   1. 访问 https://github.com/getcursor/openclaw
#   2. 按照官方文档安装
#   3. 或运行：npm install -g @openclaw/cli
#
# 不安装OpenClaw也可以正常使用，系统会使用本地模拟数据
