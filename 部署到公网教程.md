# 🌐 部署到公网 - 让全国朋友都能访问

## 方案对比

| 方案 | 优点 | 缺点 | 费用 | 推荐度 |
|------|------|------|------|--------|
| **Vercel** | 免费、快速、自动HTTPS | 需要GitHub | 免费 | ⭐⭐⭐⭐⭐ |
| **Railway** | 简单、支持Python | 有免费额度 | $5/月起 | ⭐⭐⭐⭐ |
| **阿里云/腾讯云** | 稳定、国内快 | 需要备案 | ¥100/月起 | ⭐⭐⭐ |
| **Render** | 免费、简单 | 国外服务器 | 免费 | ⭐⭐⭐⭐ |
| **Zeabur** | 国内快、支持中文 | 新平台 | 免费额度 | ⭐⭐⭐⭐⭐ |

---

## 🚀 方案1：Vercel（最推荐，免费）

### 特点
- ✅ 完全免费
- ✅ 自动HTTPS
- ✅ 全球CDN加速
- ✅ 自动部署
- ⚠️ 需要改造成Serverless架构

### 步骤

#### 1. 安装Vercel CLI
```bash
npm install -g vercel
```

#### 2. 创建vercel.json配置
```json
{
  "version": 2,
  "builds": [
    {
      "src": "web_app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "web_app.py"
    }
  ],
  "env": {
    "DEEPSEEK_API_KEY": "sk-da34a79604824fc28f73fca8381ed92f"
  }
}
```

#### 3. 部署
```bash
cd "c:\Users\陈盈桦\Desktop\自动投简历"
vercel --prod
```

#### 4. 获得域名
部署完成后会得到：`https://你的项目名.vercel.app`

---

## 🚀 方案2：Railway（推荐，简单）

### 特点
- ✅ 支持Python原生
- ✅ 免费$5额度/月
- ✅ 自动HTTPS
- ✅ 一键部署

### 步骤

#### 1. 注册Railway
访问：https://railway.app/

#### 2. 创建新项目
- 点击"New Project"
- 选择"Deploy from GitHub repo"
- 或选择"Empty Project"

#### 3. 配置环境变量
在Railway控制台添加：
```
DEEPSEEK_API_KEY=sk-da34a79604824fc28f73fca8381ed92f
```

#### 4. 创建Procfile
```bash
web: uvicorn web_app:app --host 0.0.0.0 --port $PORT
```

#### 5. 部署
- 推送代码到GitHub
- Railway自动部署
- 获得域名：`https://你的项目名.up.railway.app`

---

## 🚀 方案3：Zeabur（国内最快）

### 特点
- ✅ 国内访问快
- ✅ 支持中文界面
- ✅ 免费额度
- ✅ 简单易用

### 步骤

#### 1. 注册Zeabur
访问：https://zeabur.com/

#### 2. 创建项目
- 点击"创建项目"
- 选择"从GitHub导入"

#### 3. 配置环境变量
```
DEEPSEEK_API_KEY=sk-da34a79604824fc28f73fca8381ed92f
```

#### 4. 部署
- 自动检测Python项目
- 自动部署
- 获得域名：`https://你的项目名.zeabur.app`

---

## 🚀 方案4：阿里云/腾讯云（最稳定）

### 特点
- ✅ 国内访问最快
- ✅ 最稳定
- ⚠️ 需要备案
- ⚠️ 需要付费

### 步骤

#### 1. 购买服务器
- 阿里云ECS：https://www.aliyun.com/
- 腾讯云CVM：https://cloud.tencent.com/
- 推荐配置：2核4G，¥100/月

#### 2. 安装环境
```bash
# 连接服务器
ssh root@你的服务器IP

# 安装Python
yum install python3 python3-pip -y

# 安装依赖
pip3 install fastapi uvicorn python-dotenv openai PyPDF2 python-docx
```

#### 3. 上传代码
```bash
# 使用scp上传
scp -r "c:\Users\陈盈桦\Desktop\自动投简历" root@你的服务器IP:/root/
```

#### 4. 配置环境变量
```bash
echo "DEEPSEEK_API_KEY=sk-da34a79604824fc28f73fca8381ed92f" > /root/自动投简历/.env
```

#### 5. 启动服务
```bash
cd /root/自动投简历
nohup python3 web_app.py > app.log 2>&1 &
```

#### 6. 配置域名
- 购买域名
- 备案（需要15-20天）
- 配置DNS解析
- 配置Nginx反向代理

---

## 🚀 方案5：内网穿透（最快速）

### 特点
- ✅ 无需服务器
- ✅ 5分钟搞定
- ⚠️ 速度较慢
- ⚠️ 不稳定

### 使用ngrok

#### 1. 下载ngrok
访问：https://ngrok.com/

#### 2. 注册并获取token
```bash
ngrok authtoken 你的token
```

#### 3. 启动服务
```bash
# 终端1：启动应用
python web_app.py

# 终端2：启动ngrok
ngrok http 8000
```

#### 4. 获得公网地址
```
https://xxxx-xx-xx-xx-xx.ngrok.io
```

### 使用花生壳（国内）

#### 1. 下载花生壳
访问：https://hsk.oray.com/

#### 2. 注册账号

#### 3. 配置内网穿透
- 添加映射：本地端口8000
- 获得域名：`http://你的域名.vicp.fun`

---

## 📦 快速部署脚本

我已经为您创建了一键部署脚本！

### 使用Railway部署

```bash
# 1. 安装Railway CLI
npm install -g @railway/cli

# 2. 登录
railway login

# 3. 部署
cd "c:\Users\陈盈桦\Desktop\自动投简历"
railway init
railway up
```

### 使用Render部署

1. 访问：https://render.com/
2. 点击"New Web Service"
3. 连接GitHub仓库
4. 配置：
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn web_app:app --host 0.0.0.0 --port $PORT`
5. 添加环境变量：`DEEPSEEK_API_KEY`
6. 点击"Create Web Service"

---

## 🎯 推荐方案

### 个人测试使用
**推荐：ngrok内网穿透**
- 5分钟搞定
- 完全免费
- 适合测试

### 小规模使用（<1000用户）
**推荐：Railway或Zeabur**
- 免费额度够用
- 部署简单
- 自动HTTPS

### 大规模商业使用（>1000用户）
**推荐：阿里云/腾讯云**
- 最稳定
- 国内访问快
- 可以备案

---

## 🔧 部署后配置

### 1. 配置自定义域名
大多数平台都支持绑定自定义域名：
- Railway: Settings → Domains
- Vercel: Settings → Domains
- Zeabur: 设置 → 域名

### 2. 配置HTTPS
大多数平台自动提供HTTPS，无需配置。

### 3. 监控和日志
- Railway: 自带日志查看
- Vercel: 自带Analytics
- 阿里云: 需要配置云监控

---

## 💡 优化建议

### 1. 添加缓存
```python
from functools import lru_cache

@lru_cache(maxsize=100)
def process_resume_cached(resume_hash):
    # 缓存处理结果
    pass
```

### 2. 添加限流
```python
from slowapi import Limiter

limiter = Limiter(key_func=get_remote_address)

@app.post("/api/process")
@limiter.limit("5/minute")
async def process_resume():
    pass
```

### 3. 添加队列
```python
from celery import Celery

celery = Celery('tasks', broker='redis://localhost:6379')

@celery.task
def process_resume_async(resume_text):
    # 异步处理
    pass
```

---

## 🎉 立即开始

### 最快方案（5分钟）：

1. **下载ngrok**
   ```bash
   # 访问 https://ngrok.com/download
   ```

2. **启动应用**
   ```bash
   python web_app.py
   ```

3. **启动ngrok**
   ```bash
   ngrok http 8000
   ```

4. **分享链接**
   ```
   https://xxxx.ngrok.io
   ```

**完成！全国朋友都能访问了！** 🎊

