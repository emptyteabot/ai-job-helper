# 🌐 24小时在线部署指南

## 🎯 部署目标
将AI求职助手部署到云服务器，实现24小时在线，全球可访问（包括中国）

---

## 🚀 推荐方案

### 方案1: Railway (推荐) ⭐⭐⭐⭐⭐
**优点**: 
- ✅ 免费额度充足 ($5/月免费)
- ✅ 自动HTTPS
- ✅ 中国可访问
- ✅ 部署简单
- ✅ 自动扩展

**步骤**:

#### 1. 准备文件
创建 `railway.json`:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python web_app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

创建 `Procfile`:
```
web: python web_app.py
```

创建 `runtime.txt`:
```
python-3.11
```

#### 2. 部署到Railway
```bash
# 1. 安装Railway CLI
npm i -g @railway/cli

# 2. 登录
railway login

# 3. 初始化项目
railway init

# 4. 部署
railway up

# 5. 添加域名
railway domain
```

#### 3. 配置环境变量
在Railway Dashboard中添加:
```
DEEPSEEK_API_KEY=your_api_key_here
PORT=8000
```

#### 4. 访问
Railway会自动分配域名，如: `https://your-app.railway.app`

---

### 方案2: Render (备选) ⭐⭐⭐⭐
**优点**:
- ✅ 免费额度 (750小时/月)
- ✅ 自动HTTPS
- ✅ 中国可访问
- ✅ GitHub集成

**步骤**:

#### 1. 创建 `render.yaml`
```yaml
services:
  - type: web
    name: ai-job-helper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_app.py
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: PORT
        value: 8000
```

#### 2. 部署
1. 访问 https://render.com
2. 连接GitHub仓库
3. 选择项目
4. 点击"Deploy"

#### 3. 配置环境变量
在Render Dashboard中添加 `DEEPSEEK_API_KEY`

---

### 方案3: Vercel (前端) + Railway (后端) ⭐⭐⭐⭐⭐
**最佳方案**: 前后端分离，性能最优

#### 前端部署到Vercel
```bash
# 1. 安装Vercel CLI
npm i -g vercel

# 2. 部署
vercel --prod
```

#### 后端部署到Railway
按照方案1操作

---

### 方案4: 阿里云/腾讯云 (国内最佳) ⭐⭐⭐⭐⭐
**优点**:
- ✅ 中国访问速度快
- ✅ 稳定性高
- ✅ 可备案
- ✅ 企业级

**步骤**:

#### 1. 购买服务器
- 阿里云ECS: https://www.aliyun.com/product/ecs
- 腾讯云CVM: https://cloud.tencent.com/product/cvm
- 配置: 2核4G，带宽5M (约¥100/月)

#### 2. 安装环境
```bash
# 连接服务器
ssh root@your_server_ip

# 安装Python
sudo apt update
sudo apt install python3.11 python3-pip nginx -y

# 安装依赖
pip3 install -r requirements.txt
```

#### 3. 配置Nginx
创建 `/etc/nginx/sites-available/ai-job-helper`:
```nginx
server {
    listen 80;
    server_name your_domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
    
    location /ws {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }
}
```

#### 4. 启用站点
```bash
sudo ln -s /etc/nginx/sites-available/ai-job-helper /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 5. 配置HTTPS (Let's Encrypt)
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your_domain.com
```

#### 6. 使用Supervisor管理进程
创建 `/etc/supervisor/conf.d/ai-job-helper.conf`:
```ini
[program:ai-job-helper]
directory=/root/自动投简历
command=python3 web_app.py
autostart=true
autorestart=true
stderr_logfile=/var/log/ai-job-helper.err.log
stdout_logfile=/var/log/ai-job-helper.out.log
```

启动:
```bash
sudo supervisorctl reread
sudo supervisorctl update
sudo supervisorctl start ai-job-helper
```

---

## 📝 部署前准备

### 1. 修改 `web_app.py`
```python
# 在文件末尾修改
if __name__ == "__main__":
    port = int(os.getenv("PORT", 8000))
    print(f"\n🚀 服务启动在端口 {port}")
    uvicorn.run(app, host="0.0.0.0", port=port)
```

### 2. 创建 `requirements.txt`
```txt
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
openai==1.3.0
PyPDF2==3.0.1
python-docx==1.1.0
Pillow==10.1.0
pytesseract==0.3.10
websockets==12.0
```

### 3. 创建 `.gitignore`
```
.env
__pycache__/
*.pyc
output/
.DS_Store
```

### 4. 创建 `README.md`
```markdown
# AI求职助手

## 在线访问
https://your-domain.com

## 本地运行
```bash
pip install -r requirements.txt
python web_app.py
```
```

---

## 🎯 快速部署 (Railway - 5分钟)

### 一键部署脚本
创建 `deploy.sh`:
```bash
#!/bin/bash

echo "🚀 开始部署到Railway..."

# 1. 检查Railway CLI
if ! command -v railway &> /dev/null; then
    echo "安装Railway CLI..."
    npm i -g @railway/cli
fi

# 2. 登录
railway login

# 3. 创建项目
railway init

# 4. 添加环境变量
echo "请输入DeepSeek API Key:"
read api_key
railway variables set DEEPSEEK_API_KEY=$api_key

# 5. 部署
railway up

# 6. 获取域名
railway domain

echo "✅ 部署完成！"
```

运行:
```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 🌐 域名配置

### 1. 购买域名
- 阿里云: https://wanwang.aliyun.com
- 腾讯云: https://dnspod.cloud.tencent.com
- GoDaddy: https://www.godaddy.com

### 2. 配置DNS
添加A记录或CNAME记录指向服务器IP或Railway域名

### 3. 配置HTTPS
Railway和Render自动配置HTTPS，无需手动操作

---

## 📊 监控与维护

### 1. 日志查看
```bash
# Railway
railway logs

# Render
在Dashboard查看

# 阿里云
tail -f /var/log/ai-job-helper.out.log
```

### 2. 性能监控
- Railway: 内置监控
- Render: 内置监控
- 阿里云: 云监控服务

### 3. 自动重启
所有方案都支持自动重启，无需担心崩溃

---

## 💰 成本对比

| 方案 | 月费用 | 流量 | 性能 | 中国访问 |
|------|--------|------|------|----------|
| Railway | $5 (免费) | 100GB | ⭐⭐⭐⭐ | ✅ |
| Render | $0 (免费) | 100GB | ⭐⭐⭐⭐ | ✅ |
| Vercel+Railway | $5 | 无限 | ⭐⭐⭐⭐⭐ | ✅ |
| 阿里云 | ¥100 | 500GB | ⭐⭐⭐⭐⭐ | ✅✅ |

---

## 🎯 推荐方案总结

### 个人测试/演示
**Railway** - 免费、简单、快速

### 小规模运营
**Render** - 免费、稳定

### 中等规模
**Vercel + Railway** - 性能最优

### 大规模/企业
**阿里云/腾讯云** - 稳定、快速、可备案

---

## 🚀 立即部署

### 最快方式 (Railway)
```bash
# 1. 安装CLI
npm i -g @railway/cli

# 2. 登录
railway login

# 3. 部署
railway init
railway up

# 4. 获取域名
railway domain
```

**5分钟后，您的应用就在线了！** 🎉

---

## 📞 需要帮助？

- Railway文档: https://docs.railway.app
- Render文档: https://render.com/docs
- Vercel文档: https://vercel.com/docs
- 阿里云文档: https://help.aliyun.com

---

**部署完成后，您的AI求职助手将24小时在线，全球可访问！** 🌐✨

